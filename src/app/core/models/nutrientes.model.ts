/** Totais nutricionais agregados — mesmo shape devolvido por Dieta e Registro. */
export interface NutrientesTotais {
  proteina: number;
  gordura: number;
  caloria: number;
  carbo: number;
  qtd: number;
}
