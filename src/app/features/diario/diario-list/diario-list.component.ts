import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Placeholder de rota — implementação completa é a Fase 5 do Plano B (CLAUDE.md). */
@Component({
  selector: 'vtp-diario-list',
  standalone: true,
  template: `<p>Diário alimentar — Fase 5 do plano ainda não implementada.</p>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class DiarioListComponent {}
